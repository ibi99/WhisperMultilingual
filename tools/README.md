# Development tools

The files in this directory are development/QA helpers and are not required for normal use of WhisperMultilingual.

`language_batch_test.py` can compare automatic language detection against manually prepared `.languages.txt` files in the configured video directory.
